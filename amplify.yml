version: 1
frontend:
  phases:
    build:
      commands:
        - npm run build
    postBuild:
      commands:
        - npm run test-ci
  artifacts:
    baseDirectory: /dist/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
